import React from 'react'
import ReactDOM from 'react-dom/client'
import Providing from './Providing.tsx'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <Providing />
  </React.StrictMode>,
)
